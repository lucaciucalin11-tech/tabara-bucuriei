services:
  - type: web
    name: tabara-bucuriei
    env: docker
    envVars:
      - key: DATA_DIR
        value: /var/data
      - key: REACT_APP_ADMIN_PASS
        generateValue: false
    disk:
      name: tabara-data
      mountPath: /var/data
      sizeGB: 1
